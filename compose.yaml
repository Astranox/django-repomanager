# docker-compose.yaml
version: '3.7'

services:
  packagearchive:
    build: .
    image: &img "packagearchive:latest"
    environment: &env
      - CSRF_TRUSTED_ORIGINS=https://localhost
    restart: 'no'
    ports:
      - "8000:8000"
    volumes:
      - ./:/usr/src/app/:z
